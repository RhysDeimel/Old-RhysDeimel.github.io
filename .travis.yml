language: python
python:
  - "3.6"
branches:
  only:
    - src
install:
  - pip install -r requirements.txt
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local-dir: output
  target-branch: master
  keep_history: true
  verbose: true